
  # Landing Page for StartBusiness

  This is a code bundle for Landing Page for StartBusiness. The original project is available at https://www.figma.com/design/x6IOLGmHyPAGMuSKeprCNN/Landing-Page-for-StartBusiness.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  